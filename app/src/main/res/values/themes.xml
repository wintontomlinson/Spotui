<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Solo" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="splashScreenTheme" parent="Theme.SplashScreen">
        <!-- The splash shows the launcher icon: the dark waveform foreground on the amber
             plate colour, so the launch flash matches the icon the user tapped. -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Solo</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
</resources>

